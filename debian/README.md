# sikalabs/debian image

Debian with curl and ca-certificates

## Build

Build `sikalabs/debian`

```
make build
```

## Publish

Push `sikalabs/debian` and alias (`ondrejsika/debian`)

```
make push
```
